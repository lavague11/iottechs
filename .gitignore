node_modules/
data/
*.log
.DS_Store
desktop.ini
config.json

# Secrets — never commit env files (API keys live here)
.env
.env.*
!.env.example

# Build output
.next/
dist/

# Local databases (customer data stays local)
*.db
*.db-journal
*.db-wal
*.db-shm

# Tooling / session state — generated, not source
.claude/
.claude-flow/
.swarm/
graphify-out/
*.jsonl

# Stray shell-mishap artifacts — files accidentally created when a shell mis-parses
# inline `node -e` / `python -c` snippets (parens/braces/redirects become filenames).
# No real source file starts with "(" or "{" or is literally a code fragment.
dashboard/a.role
(*
{*
console.log*
sample-proposal.pdf
