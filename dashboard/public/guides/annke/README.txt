Annke Vision setup screenshots for the Mobile App Setup guide.

Drop the real screenshots here with these exact names (they render inside the
phone mockup, in order). Any missing file falls back to an animated scene, so
add them as you get them:

  01-appstore.png     App Store page (the "Get / Open" screen)
  02-login.png        Login / Register screen
  03-device-home.png  Device home (empty) with the "Add Device" button
  04-add-menu.png     The "Scan QR Code / Add Device Manually / Online Device" sheet
  05-scan.png         The QR / SN scanner screen
  06-connecting.png   (optional) device connecting
  07-code.png         (optional) verification-code entry
  08-name.png         (optional) name-a-camera screen
  09-alerts.png       (optional) notifications permission
  10-live.png         (optional) live view
  11-done.png         (optional) finished / device list

Recommended: portrait PNG, ~1080px wide. Step text lives in the guide data
(editable from the Support page), not here.
